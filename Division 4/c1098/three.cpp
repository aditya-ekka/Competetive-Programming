#include<iostream>
using namespace std;
#define ll long long

void adie()
{
	 ll a, n;
	 ll d[2];
	 cin >> a >> n >> d[0] >> d[1];
	 ll smaller=0;
	 ll larger=0;

	 cout << to_string(a).length() << endl;
}

int main()
{
	 int t;
	 cin >> t;
	 while(t--)
		  adie();
	 return 0;
}
